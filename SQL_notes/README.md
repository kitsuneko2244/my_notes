#SQLの考え方
- 式が複雑になりそうな場合、SQL文をいきなり書くのではなくほしいデータを書き起こして、図などに書き起こす。
- GROPUP BYなどは粒度を明確にすること。(どの列を絞り、どの列を残すのか)
- 結果の形を意識してSQLを組むこと

#よく使う形
- CTE 一時テーブルのようなもの
  WHIT
  テーブル名 AS (
  セレクト条件~
  )
- Distinct 指定した重複データを拾わない
- order by カラム名 desc 降順で表示
